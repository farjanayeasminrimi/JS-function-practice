function isEven(num) {
  if (num % 2 === 0) {
    return true;
  }
  return false;
}
// console.log(isEven(45));

/* Do sum of an array */

let numbers = [2, 2, 6, 7];
function doSum(number) {
  let sum = 0;
  for (number of numbers) {
    sum += number;
  }
  return sum;
}
let total = doSum(numbers);
console.log(total);
